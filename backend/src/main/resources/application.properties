spring.application.name=backend

spring.datasource.url=${SPRING_FINANCE_URL}
spring.datasource.username=${SPRING_FINANCE_USERNAME}
spring.datasource.password=${SPRING_FINANCE_PASSWORD}

spring.datasource.driver-class-name=org.postgresql.Driver